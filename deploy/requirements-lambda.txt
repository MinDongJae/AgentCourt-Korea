# Lambda 컨테이너용 (정적 페이지 서빙은 S3+CF, Lambda는 /api/* 백엔드만)
fastapi==0.119.1
mangum==0.19.0
pydantic==2.10.6
httpx==0.28.1

# RAG (Lambda 컨테이너에 cc 없음 → 사전 빌드 wheel만)
faiss-cpu==1.9.0.post1
numpy==1.26.4  # Python 3.11 wheel 보장

# LLM
openai==2.16.0
anthropic==0.76.0

# (pypdf는 Lambda에 불필요 — 청킹은 로컬에서 1회만 함)
